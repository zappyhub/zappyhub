# README do projeto

Fazer bonitinho dps, mas agora tô só colocando recado ;)

.ENV no ambiente local, usando o docker como passei para vcs vocês colocam essas variáveis:
```env
DATABASE_URL="postgresql://postgres:admin@localhost:5432/zappyhub?schema=public"
REDIS_URL="redis://:admin123@localhost:6379/0"
```

## Enrolação 100%

Esse commit é só para fazer o que eu falei das branchs de dev e feature!

## Crypto
Para gerar o campo de cypto, use o seguinte comando
```bash
  openssl rand -hex 32
```

Que gera automaticamente o crypto para você.